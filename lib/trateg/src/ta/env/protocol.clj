; Env protocol?
; (add algo calendar-interval asset)
; (Get-series window calendar-interval asset)
; Result-stream

; the idea of having multiple envirnemnts is that adding algos and getting their results
; should  be independent of the enviroment itself.
